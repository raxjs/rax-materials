{
  "targets": ["<%- targets.join('", "') %>"],
  "webpack5": true<%_ if (mpa && targets.includes('web')) { -%> , 
  "web": {
    "mpa": true<%_ if (pha) { -%> ,
    "pha": true<%_ } %>
  }<%_ } %><%_ if (mpa && targets.includes('weex') ) { -%> , 
  "weex": {
    "mpa": true
  }<%_ } %><%_ if (mpa && targets.includes('kraken') ) { -%> , 
  "kraken": {
    "mpa": true
  }<%_ } %>
}
