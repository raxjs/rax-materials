{
  "targets": ["<%- targets.join('", "') %>"]<%_ if (mpa && targets.includes('web')) { -%> , 
  "web": {
    "mpa": true<%_ if (mpa && targets.includes('weex') ) { -%> ,
    "pha": true<%_ } %>
  }<%_ } %><%_ if (mpa && targets.includes('weex') ) { -%> , 
  "weex": {
    "mpa": true
  }<%_ } %><%_ if (mpa && targets.includes('kraken') ) { -%> , 
  "kraken": {
    "mpa": true
  }<%_ } %>
}
